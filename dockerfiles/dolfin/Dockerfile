FROM bamboo-base

USER root

# Overwrite welcome message
RUN echo "FEniCS Bamboo image for testing of ${bamboo_planRepository_name}-${bamboo_planRepository_branchName}" > ${HOME}/WELCOME && \
    echo "Generated by ${bamboo_buildResultsUrl}" >> ${HOME}/WELCOME

USER fenics

# Add repositories
#COPY dijitso ${HOME}/build/src/dijitso
#COPY dolfin  ${HOME}/build/src/dolfin
#COPY ffc     ${HOME}/build/src/ffc
#COPY fiat    ${HOME}/build/src/fiat
#COPY instant ${HOME}/build/src/instant
#COPY ufl     ${HOME}/build/src/ufl

# Copy scripts
COPY bin ${HOME}/bin

# Set permissions
RUN sudo chown -R fenics:fenics ${HOME}
